<%= form_for(@recipe_product_list) do |f| %>
  <% if @recipe_product_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe_product_list.errors.count, "error") %> prohibited this recipe_product_list from being saved:</h2>

      <ul>
      <% @recipe_product_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
