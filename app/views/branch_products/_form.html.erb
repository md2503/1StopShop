<%= form_with(model: branch_product, local: true) do |form| %>
  <% if branch_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(branch_product.errors.count, "error") %> prohibited this branch_product from being saved:</h2>

      <ul>
      <% branch_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :branch_id %>
    <%= form.select :branch_id, options_from_collection_for_select(Branch.all, 'id', 'name'), class: "form-control"%>
  </div>

  <div class="form-group">
    <%= form.label :product_id %>
    <%= form.select :product_id, options_from_collection_for_select(Product.all, 'id', 'name'), class: "form-control"%>
  </div>

  <div class="form-group">
    <%= form.label :stock %>
    <%= form.text_field :stock, id: :branch_product_stock, class: "form-control" %>
  </div>

  <div class="form-actions">
    <%= form.submit, class: 'btn btn-primary text-uppercase' %>
  </div>
<% end %>
