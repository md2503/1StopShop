<p id="notice"><%= notice %></p>

<h1>Branch Products</h1>

<table>
  <thead>
    <tr>
      <th>Branch</th>
      <th>Product</th>
      <th>Stock</th>
      <th>Aisleno</th>
      <th>Sectionno</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @branch_products.each do |branch_product| %>
      <tr>
        <td><%= branch_product.branch_id %></td>
        <td><%= branch_product.product_id %></td>
        <td><%= branch_product.stock %></td>
        <td><%= branch_product.aisleno %></td>
        <td><%= branch_product.sectionno %></td>
        <td><%= link_to 'Show', branch_product %></td>
        <td><%= link_to 'Edit', edit_branch_product_path(branch_product) %></td>
        <td><%= link_to 'Destroy', branch_product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Branch Product', new_branch_product_path %>
