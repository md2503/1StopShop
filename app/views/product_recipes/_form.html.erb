<%= form_with(model: product_recipe, local: true) do |form| %>
  <% if product_recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_recipe.errors.count, "error") %> prohibited this product_recipe from being saved:</h2>

      <ul>
      <% product_recipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :product_id %>
    <%= form.number_field :product_id, id: :product_recipe_product_id, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :recipe_id %>
    <%= form.number_field :recipe_id, id: :product_recipe_recipe_id, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :product_recipe_amount, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :measurement %>
    <%= form.text_field :measurement, id: :product_recipe_measurement, class: "form-control" %>
  </div>

  <div class="form-actions">
    <%= form.submit :class => 'btn btn-primary text-uppercase'%>
  </div>
<% end %>
